server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA 路由回退：Vue Router 的前端路由（如 /login、/dashboard）在磁盘上没有对应文件，
    # 一律回退到 index.html 由前端 JS 接管路由，否则 nginx 会直接 404。
    location / {
        try_files $uri $uri/ /index.html;
    }
}
